# CryptoLuck Bot Configuration - Production Ready

# ===== BOT CONFIGURATION =====
API_TOKEN=your_telegram_bot_token_here
ADMIN_ID=123456789
# Can be @channel_username or numeric chat ID like -100xxxxxxxxxx
ANNOUNCE_CHANNEL_ID=@your_channel_username

# ===== DATABASE =====
DATABASE_URL=sqlite+aiosqlite:///./cryptoluck.db
# For production use PostgreSQL:
# DATABASE_URL=postgresql+asyncpg://user:password@localhost/cryptoluck

# ===== LOTTERY SETTINGS =====
PRIZE_USD=10.0
# Price per ticket (USD)
TICKET_PRICE_USD=0.10
DRAW_DAY_OF_WEEK=thu
DRAW_HOUR=20
DRAW_MINUTE=0
# IANA timezone for scheduling (e.g., America/Caracas)
DRAW_TIMEZONE=America/Caracas

# ===== BITCOIN HASH FETCH (randomness source) =====
# Preferred: comma-separated list; fallback to single URL; if unset, defaults are used.
# BITCOIN_HASH_API_URLS=https://blockstream.info/api/blocks/tip/hash,https://api.blockcypher.com/v1/btc/main/blocks/latest,https://blockchain.info/latestblock
# BITCOIN_HASH_API_URL=https://blockstream.info/api/blocks/tip/hash
# Cache TTL in seconds (default: 60)
BITCOIN_HASH_CACHE_TTL_SEC=60
# Optional: BlockCypher token to raise rate limits
BLOCKCYPHER_TOKEN=your_blockcypher_token
# Optional: custom HTTP User-Agent for external requests
HTTP_USER_AGENT=CryptoLuckBot/1.0 (+https://yourdomain.com) httpx

# ===== PAYOUT CONFIGURATION =====
# Choose currency and provide a validation regex for wallet addresses
PAYOUT_CURRENCY_CODE=XRP
PAYOUT_CURRENCY_NAME=Ripple
PAYOUT_WALLET_VALIDATION_REGEX=^r[a-zA-Z0-9]{24,34}$

# ===== NOWPAYMENTS API =====
NOWPAYMENTS_API_KEY=your_nowpayments_api_key
NOWPAYMENTS_IPN_SECRET=your_nowpayments_ipn_secret
# Public HTTPS host where the bot/app is accessible (used for links and callbacks)
SERVER_HOSTNAME=https://yourdomain.com
# IPN (webhook) endpoint URL (must be reachable by NOWPayments)
IPN_CALLBACK_URL=https://yourdomain.com/nowpayments-ipn
# Optional: redirect URLs (if you expose payment pages)
SUCCESS_URL=https://yourdomain.com/paid/success
CANCEL_URL=https://yourdomain.com/paid/cancel

# Supported deposit currencies (comma-separated), used by payment flow/UI
SUPPORTED_CURRENCIES=XRP,TRX,HBAR

# ===== HOME WALLETS (where excess funds go) =====
HOME_WALLET_TRX=TYourTronWalletAddress
HOME_WALLET_XRP=rYourRippleWalletAddress
HOME_WALLET_HBAR=0.0.xxxxxxx
# If you use other networks, add them here and ensure payout logic supports them.
# HOME_WALLET_USDTTRC20=TYourTetherTRC20WalletAddress
# HOME_WALLET_DASH=XYourDashWalletAddress
# HOME_WALLET_USDCSOL=YourSolanaWalletAddress
